apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocpv-quickstart.fullname" . }}
  labels:
    {{- include "ocpv-quickstart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocpv-quickstart.selectorLabels" . | nindent 4 }}
