apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocpv-quickstart.fullname" . }}-test-connection"
  labels:
    {{- include "ocpv-quickstart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocpv-quickstart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
